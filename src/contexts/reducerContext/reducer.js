import { types } from "./types"

export const reducer = (state, action) => {
    const {
        TEST_LOGIN
    } = types

    switch (action.type) {
        case TEST_LOGIN:
            break;

        default:
            break;
    }
}